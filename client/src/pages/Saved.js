import React from 'react';
import SavedBooks from '../components/SavedBooks'

function Saved(){
    return(
        <div class="container">
            <SavedBooks />
        </div>
    );
}

export default Saved;